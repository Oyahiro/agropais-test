/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: "anvjdytvexdhslzdbgoc.supabase.co",
            },
        ],
    },
};

export default nextConfig;
